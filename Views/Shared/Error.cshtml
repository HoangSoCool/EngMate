@model ErrorViewModel
@{
    ViewData["Title"] = "Đã xảy ra lỗi";
}

<div class="container mt-5">
    <div class="card border-danger">
        <div class="card-header bg-danger text-white">
            <h2>Đã xảy ra lỗi</h2>
        </div>
        <div class="card-body">
            <div class="alert alert-warning">
                <p><strong>Mã yêu cầu:</strong> <code>@Model.RequestId</code></p>
                
                @if (!string.IsNullOrEmpty(ViewBag.ErrorMessage))
                {
                    <hr />
                    <div class="alert alert-danger">
                        <p><strong>Chi tiết lỗi:</strong> @ViewBag.ErrorMessage</p>
                    </div>
                }
                
                <div class="mt-3">
                    <h5>Các bước xử lý có thể thực hiện:</h5>
                    <ul>
                        <li>Kiểm tra kết nối MongoDB</li>
                        <li>Kiểm tra biến môi trường MONGODB_CONNECTION_STRING đã được thiết lập trên Render</li>
                        <li>Đảm bảo IP của Render đã được thêm vào whitelist của MongoDB Atlas</li>
                    </ul>
                </div>
            </div>
            
            <div class="text-center mt-4">
                <a href="/" class="btn btn-primary">Thử lại</a>
            </div>
        </div>
    </div>
</div>
